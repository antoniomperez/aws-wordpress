version: 0.2

phases:
  install:
    commands:
      - npm install -g npm
      - yarn install
  build:
    commands:
      - yarn test
      - yarn cdk synthesize PipelineTestStack
      - yarn bump
      - git push --follow-tags origin develop

artifacts:
  base-directory: cdk.out
  files:
    - '**.*'
